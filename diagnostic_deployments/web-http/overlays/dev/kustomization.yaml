apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: diagnostic-web-dev

bases:
  - ../../base

namePrefix: dev-

commonLabels:
  environment: dev

patchesStrategicMerge:
  - replica-patch.yaml
