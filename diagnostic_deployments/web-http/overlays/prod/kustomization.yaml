apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: diagnostic-web-prod

bases:
  - ../../base

namePrefix: prod-

commonLabels:
  environment: prod

patchesStrategicMerge:
  - replica-patch.yaml
