apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: blog-app-kustomize
  namespace: argocd
  labels:
    app: blog-app
    deployment-method: kustomize
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: git@github.com:adsz/devops-lab-repo-k8s.git
    targetRevision: WIP
    path: diagnostic_deployments/demo-app-blog/k8s/kustomize/overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: k-demo-blog

  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    - PruneLast=true
    automated:
      prune: false
      selfHeal: false
    # Manual sync - odkomentuj automated żeby włączyć auto-sync
    # automated:
    #   prune: true
    #   selfHeal: true

  revisionHistoryLimit: 3
