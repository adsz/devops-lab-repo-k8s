apiVersion: v1
kind: Service
metadata:
  name: blog-frontend
  namespace: demo-blog
  labels:
    app: blog-frontend
    component: frontend
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: blog-frontend
