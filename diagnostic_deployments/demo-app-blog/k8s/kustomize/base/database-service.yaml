apiVersion: v1
kind: Service
metadata:
  name: blog-db
  namespace: demo-blog
  labels:
    app: blog-db
    component: database
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: blog-db
  clusterIP: None
