apiVersion: v1
kind: Service
metadata:
  name: blog-api
  namespace: demo-blog
  labels:
    app: blog-api
    component: api
spec:
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30050
    protocol: TCP
    name: http
  selector:
    app: blog-api
